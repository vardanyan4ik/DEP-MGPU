apiVersion: v1
kind: ConfigMap
metadata:
  name: liquidity-config
  namespace: finance-lab
data:
  APP_ENV: dev
  LOG_LEVEL: info
  APP_PORT: "8080"
  DB_HOST: db-service
  DB_PORT: "5432"
  DB_SSLMODE: disable
  FEATURE_FLAGS: "beta-ui=false,ml-cache=true"
